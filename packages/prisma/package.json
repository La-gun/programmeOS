{
  "name": "@programmeos/prisma",
  "version": "0.0.0",
  "private": true,
  "main": "./lib/index.ts",
  "types": "./lib/index.ts",
  "exports": {
    ".": "./lib/index.ts"
  },
  "scripts": {
    "build": "prisma generate && node -e \"require('fs').mkdirSync('.build',{recursive:true});require('fs').writeFileSync('.build/stamp.txt',new Date().toISOString())\"",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx seed.ts",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "prisma generate && jest --config jest.config.cjs"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "bcryptjs": "^2.4.3",
    "prisma": "^5.10.0",
    "zod": "^3.23.2"
  },
  "prisma": {
    "seed": "tsx seed.ts"
  },
  "devDependencies": {
    "@programmeos/config": "workspace:*",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.0.0",
    "dotenv": "^17.4.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}